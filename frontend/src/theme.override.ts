import { MantineThemeOverride } from "@mantine/core";

// More specific theme overrides can be added here
export const themeOverride: MantineThemeOverride = {
  components: {
    // Add component-specific styles and props here
  },
  spacing: {
    xs: "0.5rem",
    sm: "0.75rem",
    md: "1rem",
    lg: "1.5rem",
    xl: "2rem",
  },
};
